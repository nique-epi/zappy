*.o
.cache/
compile_commands.json
build*/
# The root Makefile is a tracked deliverable (subject requirement); CMake's
# generated Makefiles live under build*/ and are already ignored above.

# Compiled binaries (built into repo root via CMAKE_RUNTIME_OUTPUT_DIRECTORY)
/zappy_server
/zappy_gui
/zappy_ai


*_tests

*.pyc
__pycache__/
.coverage
.pytest_cache/
